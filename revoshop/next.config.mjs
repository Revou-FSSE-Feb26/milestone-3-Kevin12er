/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
    domains: ['placehold.co', 'i.imgur.com'],
  },

  /* config options here */
  reactCompiler: true,
};

export default nextConfig;

